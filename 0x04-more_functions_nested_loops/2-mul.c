#include "main.h"

/**
 * mul - check the code
 * @a: parameter to be checked 
 * @b: parameter to be checked
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int multiply = a * b;
	return (multiply);
}
